# Signaling server (baked into the client at build time).
# Default: host Radmin VPN IP — friends only need the release exe.
PROXCHAT_SERVER=http://26.36.227.156:3100

# CI only — set automatically by GitHub Actions (owner/repo for auto-updater).
# GITHUB_REPOSITORY=Sandrochkhaidzee/LeagueCustomProxy
