node_modules/
dist/
dist-server/
.env

# Build artifacts
*.opk

# Large generated assets (not needed for build)
assets/champion-circles/
scripts/synthetic-data/
src/data/champion-fingerprints.json

# Python
scripts/__pycache__/
*.pyc

# PyTorch checkpoint (large, ONNX model is the shipped artifact)
models/*.pt

# Rust/Tauri build artifacts
src-tauri/target/

# Server build output
server/dist/

# Webpack filesystem cache
node_modules/.cache/

# Distribution build (local only)
release/

# Local-only host helper (not shipped)
scripts/start-public-host.bat

# Temp/logs
logs.txt

# Claude Code session-local settings
.claude/

# Git worktrees (per-feature isolated checkouts)
.worktrees/
.playwright-mcp/

# scratch analysis / contact-sheet images from CV debugging
/_*.png
